#include <stdio.h>
int main()
{
	char h[5];
		printf("\nWanna do more calculation with recent output (1 for yes, 0 for no) : ");
	scanf("%c", &h);
	if ("h="yes""){
		printf("Being ready for next calculation.......");
	}
	else if(h="no"){
		printf("Quit");
	}
	else {
		printf("undefined");
	}
	return 0;
}
